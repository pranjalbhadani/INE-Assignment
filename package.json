{
  "name": "ine-assignment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:all": "npm run lint --prefix frontend && npm run type-check --prefix backend && npm run test --prefix backend && npm run test:e2e --prefix frontend && npm run build --prefix backend && npm run build --prefix frontend"
  },
  "dependencies": {
    "@playwright/test": "^1.63.0"
  }
}
